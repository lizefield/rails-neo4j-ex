<%= form_with(model: auther, local: true) do |form| %>
  <% if auther.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auther.errors.count, "error") %> prohibited this auther from being saved:</h2>

      <ul>
      <% auther.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
